import { SQLServer, StationDailyReportStore } from '../toolts'
import { stationConfig } from '../../../globalSettings'

export async function autoFillDailyReport(){
  // const __today = new Date()
  // const stations = stationConfig.stations
  // await Promise.all(stations.map(station => {
  //   StationDailyReportStore.fillDailyReport()
  // }))
}